version: 2.1

jobs:
  build:
    docker:
      - image: adoptium/adoptium-openjdk:17-jdk
    steps:
      - checkout  # Checkout your code from the repository
      - run:
          name: Check Java Version
          command: java -version
      - run:
          name: Build and Test
          command: ./gradlew test
